#include<stdio.h>

int main(){
    // iterator(iteration) ===> iska matlab kisi bhi kaam ko bhaar bhaar karna
    // for(initialisation;condition;updation) 
    for(int i=1; i<=50; i += 1) {
        printf("%d-printing hello world fifty times!!\n",i);
    }
    return 0;
}

// shortcut to write 
// i = i + 1 ===> i++(adding 1 ) if adding 2 then {i+2} || i +=
