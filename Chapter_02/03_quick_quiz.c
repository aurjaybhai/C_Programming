#include<stdio.h>
#include<math.h>

int main() {
    int a = (int)1.999999; // here (int) is converting the 1.9999 into 1 and it is 
// explicit conversion mean we have to convert not an implicit conversion(automatic) 
    printf("%d\n", a);
    return 0;

}

