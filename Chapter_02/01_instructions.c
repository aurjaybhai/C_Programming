#include<stdio.h>

int main() {
    int a = 22;
    int b = a;
    int c = b * 5;
    int d = 1, e;
    
    // int old_Age = 22;
    // int new_Age = old_Age + years; 
    // int years = 2; // years is defined used afterwards of use so it's undefined so shift
    // before line 10

// First you  have to declare then use the variable

    int x,y,z;
    int x = y = z = 1;
    
}

